@page "/product/edit/{id}"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]

<EditorProduct Mode=ProductEditMode.Edit Id=@Id/>

@code
{
    [Parameter]
    public string Id { get; set; }
}